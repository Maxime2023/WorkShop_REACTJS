import React, { Component } from 'react';
// créer le fichier Description.css import './Description.css'
class Description extends Component {
    state = {
        // pareil que dans app.js creer un variable d'etat qui va stocker les infos
      }
    componentDidMount() {
        //Quand la classe description est appellée, il faut mettre a jour la variable d'état qui va contenir l'information
    }
    render() {
        return (
            <div>Description</div>
        );
    }
}

export default Description;
